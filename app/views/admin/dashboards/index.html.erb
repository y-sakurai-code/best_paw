<div class="content">
  <h1>全ユーザー一覧</h1>
    <ul>
      <% @users.each do |user| %>
        <li>
          <%= link_to user.name, admin_user_path(user.id) %>
        </li>
      <% end %>
    </ul>
</div>